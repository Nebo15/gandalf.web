nav.main-navbar.navbar.navbar-static-top.navbar-default
  .container
    .navbar-header
      button.navbar-toggle.collapsed(type='button',
        data-toggle='collapse',
        data-target='#navbar-menu',
        aria-expanded='false'
        )
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='#') Gandalf

    #navbar-menu.collapse.navbar-collapse
      ul.nav.navbar-nav
        li.dropdown(uib-dropdown)
          a.dropdown-toggle(role="button", uib-dropdown-toggle) {{project.title}}
            | &#32;
            b.caret
          ul.dropdown-menu
            li(ng-repeat="item in projects", ng-class="{'active': project.id == item.id}"): a(ng-click="selectProject(item)") {{item.title}}
            li.divider(role="separator")
            li: project-add + add new project

        li(ui-sref-active="active"): a(ui-sref="tables-list") Tables
        li(ui-sref-active="active"): a(ui-sref="history-list") History
        // li(ui-sref-active="active"): a(ui-sref="groups-list") Groups
      ul.nav.navbar-nav.navbar-right
        li.dropdown(uib-dropdown)
          a.dropdown-toggle(role="button", uib-dropdown-toggle) {{user.username}}
            | &#32;
            b.caret
          ul.dropdown-menu
            li.dropdown-header
              b {{user.username}}
              br
              span {{user.email}}
            li.divider(role="separator")
            li
              a(ng-click="logout()") Sign Out
