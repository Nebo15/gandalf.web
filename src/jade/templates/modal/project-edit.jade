form(ng-submit="save(form)", name="form", novalidate)
  .modal-header
    h3.modal-title Edit project info
  .modal-body
    .form-horizontal
      .form-group
        label.col-sm-3.control-label Name
        .col-sm-9: input.form-control(ng-model="project.title", required)
      .form-group
        label.col-sm-3.control-label Description
        .col-sm-9: input.form-control(ng-model="project.description", placeholder="e.g. Tell a few words about the project")
  .modal-footer.clearfix
    .pull-right
      button.btn.btn-warning(type='button', ng-click='cancel()') Cancel
      button.btn.btn-primary(type='submit') Save
