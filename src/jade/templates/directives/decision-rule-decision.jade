div(ng-if="!isEditing").form-control-static {{rule.than}}
div(ng-if="isEditing")
  span(ng-if="type == matchingTypes.first")
    decision-input(type="type", decisions="decisions", model="rule.than", placeholder="Decision")
  span(ng-if="type == matchingTypes.all")
    decision-input(type="type", decisions="decisions", model="rule.than", placeholder="Score")
